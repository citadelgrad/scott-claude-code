{
  "id": "capacity-and-stable-ordering",
  "description": "Three disjoint-scope candidates with mixed priorities and a hard cap of 2, evaluated with the priority-then-id fallback order (live_ready_order=false). D and F tie at priority 1 and break by id bytes; E is the lowest-priority candidate and overflows the cap.",
  "ac": ["AC-T15-004"],
  "hard_cap": 2,
  "live_ready_order": false,
  "ready": [
    { "id": "E", "priority": 3, "dependency_ids": [], "owned_paths": ["src/e.py"] },
    { "id": "D", "priority": 1, "dependency_ids": [], "owned_paths": ["src/d.py"] },
    { "id": "F", "priority": 1, "dependency_ids": [], "owned_paths": ["src/f.py"] }
  ],
  "expected": {
    "selected": ["D", "F"],
    "refusal": null,
    "excluded": { "E": "CAPACITY_REACHED" }
  }
}
